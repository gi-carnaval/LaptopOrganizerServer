{
  "name": "backend",
  "version": "1.0.0",
  "description": "Servidor do aplicativo Organizador de Notebooks do SENAI Ourinhos-SP",
  "main": "index.js",
  "author": "",
  "license": "ISC",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "prisma generate && npx prisma migrate deploy && tsc"
  },
  "devDependencies": {
    "@types/node": "^20.14.4",
    "prisma": "^5.15.0",
    "tsx": "^4.15.6",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@fastify/cors": "^8.0.0",
    "@prisma/client": "^5.15.0",
    "fastify": "^4.28.0",
    "fastify-cors": "^6.1.0"
  }
}
